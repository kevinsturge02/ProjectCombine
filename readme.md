# Basic usage (extracts .c and .cpp files)
python extract_code.py path/to/zipfile.zip output.txt

# Specify custom extensions
python extract_code.py path/to/zipfile.zip output.txt --extensions .c .h .cpp .hpp